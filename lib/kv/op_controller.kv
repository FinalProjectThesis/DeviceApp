<AdditionScreen>:
    name: 'addition'
    BoxLayout:
        orientation: 'vertical'
        Label:
            id: add_label
        Button:
            text: 'Addition Page'
            on_press: root.manager.current = 'menu'

<SubtractionScreen>:
    name: 'subtraction'
    BoxLayout:
        orientation: 'vertical'
        Label:
            id: sub_label
        Button:
            text: 'Subtraction Page'
            on_press: root.manager.current = 'menu'

<MultiplicationScreen>:
    name: 'multiplication'
    BoxLayout:
        orientation: 'vertical'
        Label:
            id: multi_label
        Button:
            text: 'Multiplication Page'
            on_press: root.manager.current = 'menu'

<DivisionScreen>:
    name: 'division'
    BoxLayout:
        orientation: 'vertical'
        Label:
            id: div_label
        Button:
            text: 'Division Page'
            on_press: root.manager.current = 'menu'